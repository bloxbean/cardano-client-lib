dependencies {
    api project(':core-api')
    api project(':plutus')
    api project(':function')
    api project(':core')

    api libs.hydra
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Cardano Client Supplier - Hydra'
                description = 'Cardano Client Lib - Hydra Supplier Module'
            }
        }
    }
}
