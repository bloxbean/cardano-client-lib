dependencies {
    api project(':cardano-client-core')
    api project(':cardano-client-function')
    api project(':cardano-client-cip')
    api project(':cardano-client-backend')

    api (libs.ogmios.java)

    implementation(libs.retrofit2) {
        exclude group: 'com.squareup.okhttp3', module: 'okhttp'
    }
    implementation(libs.retrofit2.converter.jackson) {
        exclude group: 'com.fasterxml.jackson.core', module: 'jackson-databind'
    }
    implementation(libs.okhttp)
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Cardano Client Ogmios Backend'
                description = 'Cardano Client Lib - Ogmios Backend Module'
            }
        }
    }
}
