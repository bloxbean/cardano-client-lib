dependencies {
    api project(':cardano-client-core')
    api project(':cardano-client-backend')

    api (libs.koios.java)
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Cardano Client Koios Backend'
                description = 'Cardano Client Lib - Koios Backend Module'
            }
        }
    }
}

