dependencies {
    api project(':common')
    api project(':transaction-spec')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Cardano Client Core Api module'
                description = 'Cardano Client Lib - Core Api Module'
            }
        }
    }
}
